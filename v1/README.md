# file-organizer

A Python command that takes a directory and arranges it by filetype

## Usage

``` python3 file_organizer.py [directory-path] ```

## What it does

Organizes the files into their directories based on their filetype

## Limitations

- Has a fixed set of extensions and filetype directory
- All the directories are created even when no file of that type is found